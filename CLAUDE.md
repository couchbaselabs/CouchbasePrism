## Agent skills

### Issue tracker

Issues and specs live as local markdown files under `.scratch/`. See `internal/issue-tracker.md`.

### Domain docs

Single-context layout — CONTEXT.md and docs/adr/ at the repo root. See `internal/domain.md`.
