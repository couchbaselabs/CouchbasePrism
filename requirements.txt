# PRISM runtime + eval + demo. Deliberately small: PDF parsing is a fast
# cover-page pass, chunking/embedding is done by the Couchbase AI Data Plane,
# and Couchbase access is over the Query REST service.
pymupdf>=1.24
pyyaml>=6.0
requests>=2.31
streamlit>=1.35
pytest>=8.0
# S3 upload in the Setup tab (staging sample PDFs for the AI Data Plane
# workflow to ingest) - credentials are supplied through the UI at upload
# time, never read from ~/.aws or an env var, so this stays the only place
# AWS access touches PRISM.
boto3>=1.34
